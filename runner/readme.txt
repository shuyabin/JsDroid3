脚本运行器
1.展示脚本界面(如果没有，则显示logo、日志、系统信息、版本号)
2.运行脚本
3.停止脚本
4.打印日志


加载内核
1.判断系统是否root
    如果有root，则通过root执行后台初始化，跳转到3；否则，跳转到2。
2.判断是否开启adb端口(5555)
    如果开启，则通过adb端口执行后台初始化；否则，则提示并且等待用户开启。
3.等待开启服务端口：18888
    等待10秒，提示用户是否成功。
4.连接服务端口：18888
    如果连接断开，尝试重连，超过5次跳转到1。
